# 算子-论文映射

## 1. 使用说明
本文件用于回答“某个算子为什么合理”和“它在现有文献中是否有明确支撑”这两个问题。

映射规则如下：

- `P2`：可以直接对齐到论文中的核心模块或明确处理环节
- `P1`：由多篇论文中的共同需求抽象出来
- `P0`：系统闭环必需，但不是论文中的显式独立模块

## 2. 总表

| 算子 | 等级 | 主要文献 | 保留的核心能力 | 备注 |
|---|---|---|---|---|
| A1 划分算子 | P1 | Panda-70M, TimeChat-Captioner, Video-in-the-Loop | 先划定可处理时间范围，再进行理解和生成 | 最新文献更多写成 span localization 或 semantic retrieval |
| A2 上下文编排算子 | P1 | Panda-70M, Shot2Story, TimeChat-Captioner | 相邻片段拼接、叙事上下文附着、可消费单元重组 | 常以内嵌链路出现，而不是单独命名 |
| A3 采样算子 | P2 | Video-ChatGPT, Video-LLaVA, LLaVA-NeXT-Interleave, Adaptive Keyframe Sampling, STORM, AdaRD-Key, Video-in-the-Loop, Think with Grounding | 统一基础采样、关键帧选择、budget-aware 重分配 | 现在同时承担基础采样与自适应采样 |
| A4 时序证据定位算子 | P2 | LeAdQA, ED-VTG, Video-in-the-Loop, TimeChat-Captioner, Think with Grounding | moment localization、timestamped summary、关键 span 回看 | 应强调证据定位而不只是摘要 |
| A5 空间证据聚焦算子 | P2 | VideoRefer Suite, SpaceVLLM, CAT-V, VideoLoom, SlotVTG | 对象级、区域级、时空联合 grounding | 空间证据越来越重要 |
| A6 文本辅证抽取算子 | P1 | MME-VideoOCR, LongVideoBench, TimeChat-Captioner | OCR、字幕、ASR 作为文本辅证流 | 更多是证据汇聚而非独立热点 |
| A7 问题规划算子 | P1 | LongViTU, VDC-Agent, VideoRefer Suite, Think with Grounding | 题型设计、证据选择、难度控制、问题蓝图生成 | 更像 agentic data construction 中的中间步骤 |
| A8 QA生成算子 | P2 | LongViTU, VDC-Agent, Video-ChatGPT, VideoRefer Suite, TimeChat-Captioner | grounded QA 自动生成 | 最直接面向问答数据产出 |
| A9 Grounded核验与回修算子 | P2 | LongViTU, VDC-Agent, MVQA-68K, Omni-Judge | grounded 核验、多维评分、回修和拒绝决策 | 评分之外还应支持修正闭环 |

## 3. 当前文献支撑强度分组

### 3.1 强支撑算子
- A3 采样算子
- A4 时序证据定位算子
- A5 空间证据聚焦算子
- A8 QA生成算子
- A9 Grounded核验与回修算子

### 3.2 中等支撑算子
- A1 划分算子
- A2 上下文编排算子
- A6 文本辅证抽取算子
- A7 问题规划算子

## 4. 不同等级的算子定义

### P2 算子
该算子的核心能力已经被近期文献反复验证，当前的系统是在做可复用的原子化整理。

### P1 算子
该级别的算子并非某一篇论文的原生模块，但它稳定抽取了多篇论文中的共同需求。

### P0 算子
主要用于保证工程闭环和输入纯度，其必要性来自系统运行，而不是源于论文。

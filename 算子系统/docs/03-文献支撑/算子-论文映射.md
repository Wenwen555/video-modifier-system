# 算子-论文映射

## 1. 使用说明
本文件用于回答“某个算子为什么合理”和“它在现有文献中是否有明确支撑”这两个问题。

映射规则如下：

- `P2`：可以直接对齐到论文中的核心模块或明确处理环节
- `P1`：由多篇论文中的共同需求抽象出来
- `P0`：系统闭环必需，但不是论文中的显式独立模块

## 2. 总表

| 算子 | 等级 | 主要文献 | 保留的核心能力 | 备注 |
|---|---|---|---|---|
| A1 划分算子 | P1 | Panda-70M, TimeChat-Captioner, Video-in-the-Loop | 先划定可处理时间范围，再进行理解和生成 | 最新文献更多写成 span localization 或 semantic retrieval |
| A2 片段清洗算子 | P0 | MME-VideoOCR, MVQA-68K | 明确承认低质量片段会污染理解 | 主要是系统工程补全 |
| A3 采样算子 | P2 | Video-ChatGPT, Video-LLaVA, LLaVA-NeXT-Interleave | 多帧基础采样、稳定低成本输入 | 作为几乎所有高级采样方法的基线 |
| A4 自适应采样算子 | P2 | Adaptive Keyframe Sampling, STORM, AdaRD-Key, Video-in-the-Loop, Think with Grounding | relevance-aware、diversity-aware、span-aware 采样 | 2025-2026 最强趋势之一 |
| A5 去重算子 | P1 | STORM, SlowFast-LLaVA-1.5, V-CORE, Token Reduction via AOT | 冗余压缩、信息保留式 token reduction | 文献常写为压缩或 pruning |
| A6 文本抽取算子 | P1 | MME-VideoOCR, LongVideoBench, TimeChat-Captioner | OCR、字幕、ASR 作为文本证据流 | 更多是证据汇聚而非独立热点 |
| A7 空间聚焦算子 | P2 | VideoRefer Suite, SpaceVLLM, CAT-V, VideoLoom, SlotVTG | 对象级、区域级、时空联合 grounding | 空间证据越来越重要 |
| A8 时序定位与变化摘要算子 | P2 | LeAdQA, ED-VTG, Video-in-the-Loop, TimeChat-Captioner, Think with Grounding | moment localization、timestamped summary | 应同时强调定位与摘要 |
| A9 标注生成算子 | P2 | LongViTU, GROVE, VideoRefer Suite, TimeChat-Captioner, VDC-Agent | caption、grounded caption、QA、summary 自动生成 | 最直接面向数据产出 |
| A10 候选选择与质量裁决算子 | P2 | LongViTU, VDC-Agent, MVQA-68K, Omni-Judge | 多候选优选、质量打分、可解释裁决 | 评分应是多维而非单分 |

## 3. 当前文献支撑强度分组

### 3.1 强支撑算子
- A3 采样算子
- A4 自适应采样算子
- A5 去重算子
- A7 空间聚焦算子
- A8 时序定位与变化摘要算子
- A9 标注生成算子
- A10 候选选择与质量裁决算子

### 3.2 中等支撑算子
- A1 划分算子
- A6 文本抽取算子

### 3.3 系统支撑算子
- A2 片段清洗算子

## 4. 不同等级的算子定义

### P2 算子
该算子的核心能力已经被近期文献反复验证，当前的系统是在做可复用的原子化整理

### P1 算子
该级别的算子并非某一篇论文的原生模块，但它稳定抽取了多篇论文中的共同需求

### P0 算子
主要用于保证工程闭环和输入纯度，其必要性来自系统运行，而不是源于论文

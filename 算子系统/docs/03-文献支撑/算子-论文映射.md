# 算子-论文映射

## 1. 使用说明
本文件用于回答“某个算子为什么合理”和“它在现有文献中是否有明确支撑”这两个问题。

映射规则如下：

- `P2`：可以直接对齐到论文中的核心模块或明确处理环节
- `P1`：由多篇论文中的共同需求抽象出来
- `P0`：系统闭环必需，但不是论文中的显式独立模块

## 2. 总表

| 算子 | 等级 | 主要文献 | 保留的核心能力 | 备注 |
|---|---|---|---|---|
| A1 划分算子 | P1 | Panda-70M, TimeChat-Captioner | 先划定可处理时间范围，再进入后续构造 | 基础入口 |
| A2 上下文编排算子 | P1 | Panda-70M, Shot2Story, TimeChat-Captioner | 相邻片段拼接、叙事上下文附着、可消费单元重组 | 骨架前处理 |
| A3 采样算子 | P2 | Video-ChatGPT, Adaptive Keyframe Sampling, AdaRD-Key, Video-in-the-Loop | 统一基础采样、关键帧选择、预算重分配 | 基础而强支撑 |
| A4 粗Caption算子 | P1 | TimeChat-Captioner, Shot2Story, LongViTU | 片段级语义描述 | scaffold 底座 |
| A5 时序结构抽取算子 | P2 | Grounded Question-Answering in Long Egocentric Videos, Grounded Multi-Hop VideoQA in Long-Form Egocentric Videos, LongViTU, TimeChat-Captioner | 时间窗、事件链、多跳时序结构 | 当前默认主链关键算子 |
| A6 文本辅证抽取算子 | P1 | MME-VideoOCR, LongVideoBench, TimeChat-Captioner | OCR、字幕、ASR 作为骨架辅证 | 多为辅助流 |
| A7 结构骨架构建算子 | P1 | Grounded Question-Answering in Long Egocentric Videos, Grounded Multi-Hop VideoQA in Long-Form Egocentric Videos, LongViTU | chunk、tree、graph 等结构单元构建 | 当前系统中显式拆出 |
| A8 QAE共同合成算子 | P2 | Grounded Question-Answering in Long Egocentric Videos, Grounded Multi-Hop VideoQA in Long-Form Egocentric Videos, LongViTU | 在共享 scaffold 上共同生成 Question、Answer、Evidence | 数据构造主链核心 |
| A9 一致性回修算子 | P1 | LongViTU, Grounded Multi-Hop VideoQA in Long-Form Egocentric Videos, VDC-Agent | self-revision、triplet refinement、字段对齐 | 出站前修订层 |
| A10 质量筛查算子 | P2 | LongViTU, Omni-Judge, MVQA-68K | 规则过滤、评分和保留决策 | 最终出口 |

## 3. 当前文献支撑强度分组

### 3.1 强支撑算子
- A3 采样算子
- A5 时序结构抽取算子
- A8 QAE共同合成算子
- A10 质量筛查算子

### 3.2 中等支撑算子
- A1 划分算子
- A2 上下文编排算子
- A4 粗Caption算子
- A6 文本辅证抽取算子
- A7 结构骨架构建算子
- A9 一致性回修算子

## 4. 不同等级的算子定义

### P2 算子
该算子的核心能力已经被近期文献反复验证，当前系统是在做可复用的原子化整理。

### P1 算子
该级别的算子并非某一篇论文中的原生独立模块，但它稳定抽取了多篇论文中的共同需求。

### P0 算子
主要用于保证工程闭环和输入纯度，其必要性来自系统运行，而不是来自论文。
